ui_bottomnote <- function() {
  
  panel(#status = "info",
        
        HTML(
          
          "<p>
             <b>NB.</b> La carte est cliquable ! Après un clic, le champ 
             \"Entrez votre commune\"
             se remplira tout seul, mais vous devrez ensuite cliquer sur le bouton
             <b>Search</b> pour actualiser les informations.</p>
          "
        )
  )
}
